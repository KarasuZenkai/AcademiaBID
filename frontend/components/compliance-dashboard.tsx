"use client";

import { useEffect, useState } from "react";
import { getComplianceDashboard, type ComplianceDashboard } from "@/lib/api/dashboard";

export function ComplianceDashboardView() {
  const [data, setData] = useState<ComplianceDashboard | null>(null);
  const [error, setError] = useState("");
  useEffect(() => { getComplianceDashboard().then(setData).catch((item) => setError(item.message)); }, []);
  if (error) return <p className="app-card p-5 text-red-700">{error}</p>;
  if (!data) return <p>Cargando cumplimiento…</p>;
  const cards = [["Usuarios asignados", data.summary.assigned_users], ["Unidades", data.summary.unit_count], ["Cumplimiento global", `${data.summary.completion_rate_percent}%`], ["Asignaciones pendientes", data.summary.pending_assignments]];
  return <div className="space-y-7"><section className="app-gradient rounded-2xl p-6 text-white"><p className="text-sm text-cyan-200">Panel para liderazgo y Talento Humano</p><h1 className="mt-1 text-3xl font-bold">Cumplimiento de aprendizaje</h1><p className="mt-2 max-w-2xl text-sm text-white/70">Vista global demo. En la siguiente etapa, cada responsable verá únicamente las unidades que tenga asignadas.</p></section><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label, value]) => <div className="app-card p-5" key={label as string}><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-[#002952]">{value}</p></div>)}</section><section><div className="flex items-end justify-between gap-4"><div><h2 className="text-lg font-semibold text-slate-800">Cumplimiento por unidad</h2><p className="mt-1 text-sm text-slate-500">Ordenado de menor a mayor cumplimiento para facilitar el seguimiento.</p></div></div><div className="app-card mt-4 overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Unidad</th><th className="px-5 py-3">Personas</th><th className="px-5 py-3">Avance</th><th className="px-5 py-3">Completado</th><th className="px-5 py-3">Pendiente</th></tr></thead><tbody>{data.units.map((unit) => <tr className="border-b border-slate-100 last:border-0" key={unit.slug}><td className="px-5 py-4 font-semibold text-slate-800">{unit.name}</td><td className="px-5 py-4 text-slate-600">{unit.assigned_users}</td><td className="min-w-44 px-5 py-4"><div className="flex items-center gap-3"><div className="h-2 flex-1 rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#51b1db]" style={{ width: `${unit.average_progress_percent}%` }} /></div><span className="w-11 text-right text-xs font-semibold text-slate-700">{unit.average_progress_percent}%</span></div></td><td className="px-5 py-4 text-emerald-700">{unit.completed_assignments}</td><td className="px-5 py-4 text-amber-700">{unit.pending_assignments}</td></tr>)}</tbody></table></div></section><section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="app-card p-5"><h2 className="text-lg font-semibold text-slate-800">Unidades que requieren atención</h2><p className="mt-1 text-sm text-slate-500">Menor porcentaje de cumplimiento global.</p><div className="mt-4 space-y-3">{data.attention_units.map((unit) => <div className="rounded-lg bg-amber-50 p-3" key={unit.name}><p className="font-semibold text-slate-800">{unit.name}</p><p className="mt-1 text-sm text-amber-800">{unit.completion_rate_percent}% de cumplimiento · {unit.pending_assignments} pendientes</p></div>)}</div></div><div className="app-card overflow-x-auto"><div className="p-5"><h2 className="text-lg font-semibold text-slate-800">Seguimiento por persona</h2><p className="mt-1 text-sm text-slate-500">Indicadores de cumplimiento; no expone el detalle de reproducción.</p></div><table className="min-w-full text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Persona</th><th className="px-5 py-3">Unidad</th><th className="px-5 py-3">Avance</th><th className="px-5 py-3">Pendientes</th></tr></thead><tbody>{data.users.map((user) => <tr className="border-b border-slate-100 last:border-0" key={user.name}><td className="px-5 py-4 font-semibold text-slate-800">{user.name}</td><td className="max-w-52 px-5 py-4 text-xs text-slate-600">{user.units.join(", ") || "Sin unidad asignada"}</td><td className="px-5 py-4 text-slate-700">{user.average_progress_percent}%</td><td className="px-5 py-4 text-amber-700">{user.pending_courses}</td></tr>)}</tbody></table></div></section></div>;
}
